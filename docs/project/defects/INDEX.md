# Defect index

Search this file first, then open only matching defect records. Do not load the
whole directory into agent context.

| ID | Status | Area | Symptom | Record |
|---|---|---|---|---|
| DOC-2026-08-28-02 | OPEN | Switched-TS diagnostic counters; deck/report scalars | A fresh run of the `adaptive` 250 s arm reproduces the earlier cache on every published quantity -- 250.000 s, reclose 159.252, 3679 samples, 161 controller-rejected steps, residual $9.9721\times10^{-9}$, subdivision 0, terminal 60.000001 Hz, identical augment/release instants -- but `domain_rejected_trials` differs, 1027 against 1225, and is the ONLY differing line in the regenerated `run_summary_v2.tex`. Cause NOT established: either the earlier cache predates a touch to the counter's call site (so the two count different populations), or the line-search trial sequence is not bit-reproducible where its accepted iterates are; distinguishing them needs two consecutive fresh runs on one unchanged tree, not performed. Not a numerical defect: a rejected trial is discarded (`composite_newton.m:132-136` never assigns accepted state/residual/Jacobian from one) and a violation at an ACCEPTED state still aborts, so the counter describes the search path, not the trajectory. Action: the defense deck no longer prints it -- the no-gate-was-relaxed claim now rests on the residual, subdivision depth and rejected-step count, all of which reproduce -- with the decision recorded at the frame's SOURCE comment so it is not reinstated. The report still prints it, regenerated, and is left as found | [Domain-reject counter not reproducible](2026-08-28-deck-domain-reject-counter-not-reproducible.md) |
| DOC-2026-08-28-01 | RESOLVED | English defense presentation, spectrum frame | The deck printed "Slowest root $-0.174920$ (machine field flux)" while its own cited generated table gives row 35 as $-0.065196\pm0.124395\mathrm{j}$ ($0.019798$ Hz, $\zeta=0.464215$, machine speed) -- slower by real part AND modulus, so the sentence contradicted its source. Cause: the claim was written from a scan of the table's real roots only, and row 34 is the last real row before the complex pair. Corroborated by the selector table, whose machine-online base-case margin $-0.065196$ equals that pair's real part. Fixed by naming both quantities (slowest root; slowest real decay rate), keeping the "factor of three" margin sentence on the electromechanical mode, and describing row 34 as a decay rate rather than a damping. The unsourced power-flow residual claim `6.34e-15` was removed in the same pass because no generated artefact in the repository produces it. No model, parameter, event, gate or generated table changed | [Deck named the wrong slowest root](2026-08-28-deck-slowest-root-mislabel.md) |
| DOC-2026-08-27-02 | RESOLVED | English defense-presentation PDF build | MiKTeX had the `newtx` Type-1 files and map installed but its active PDF font map was stale, so both pdfLaTeX and XeLaTeX fell back to PK generation and failed. Reconfiguring font maps restored the intended Times build; two PDF builds produced 16 pages and all pages were visually inspected | [MiKTeX newtx font map stale](2026-08-27-miktex-newtx-font-map-stale.md) |
| DOC-2026-08-27-01 | RESOLVED | English defense presentation | Visual rendering found a network placeholder, clipped dense content, and an over-wide title/legend. Replaced the placeholder with the study network, refit the dense pages, regenerated slide-native figures without changing data, and removed visible code/filename language. Two PDF builds produced 16 pages; all pages were rendered and inspected | [Presentation layout and provenance](2026-08-27-presentation-layout-and-provenance.md) |
| IBR-2026-07-16-01 | RESOLVED | IEEE14 IBR hybrid TS validation | Scenario-B routing, per-island validation, transaction identity, and comparison evidence are incomplete | [Corrective validation defects](2026-07-16-ieee14-ibr-corrective-validation.md) |
| IBR-2026-07-17-01 | OPEN | Generic automatic GFM selection | Revision 5 corrective closure: event-runner migration, validator latent bugs, real timers, authenticated SG_ON routing, N_exhaustive_max guard, unpinned automatic integration. Targeted 86/86 GREEN; full regression pending | [Fixed GFM selection defects](2026-07-17-fixed-gfm-selection.md) |
| IBR-2026-07-18-01 | RESOLVED | IBR Section H reporting + stability modal analysis | struct() constructor collapsed cell-of-structs to a 1x1 struct; parentheses-on-cell indexing in modal_analysis mis-read participation status | [Cell-array collapse and indexing](2026-07-18-cell-array-collapse-and-indexing.md) |
| UI-2026-07-19-01 | RESOLVED | Analysis Wizard | Analysis could be highlighted but footer was missing; initial case was not committed; TS/IBR Events page was skipped | [Wizard footer, selection, and navigation](2026-07-19-wizard-footer-selection-navigation.md) |
| UI-2026-07-19-02 | RESOLVED_TEST_CONTRACT | IBR launcher defaults | Tests encoded the retired implicit four-WECC launcher default and required an out-of-scope RMS10 fault to converge | [IBR launcher default-profile test contract](2026-07-19-ibr-launcher-default-profile-test-contract.md) |
| IBR-2026-07-19-02 | RESOLVED | RMS10 reduced equilibrium initialization | Registered `ibr_dual_mode_rms10` devices were rejected by a legacy-only device-identity gate although they implement the required equilibrium ABI | [RMS10 reduced initializer device type](2026-07-19-rms10-reduced-initializer-device-type.md) |
| IBR-2026-07-19-03 | RESOLVED_PENDING_FINAL_REGRESSION | GFL-RMS10 balanced-fault TS | Runtime `V_valid_min` gate blocked the sourced FRT domain; fault-on rolled back near 0.377 pu | [RMS10 runtime low-voltage domain](2026-07-19-rms10-runtime-low-voltage-domain.md) |
| IBR-2026-07-19-04 | RESOLVED | SG-on all-GFL equilibrium/SSSA/TS | Mode-aware PF used constant-power loads while the composite DAE used frozen constant-admittance loads, leaving a 0.164 pu KCL component | [SG-on all-GFL equilibrium](2026-07-19-sg-on-all-gfl-equilibrium.md) |
| IBR-2026-07-19-05 | DOMAIN_THROW_RESOLVED / END_TO_END_GATE_BLOCKED | Domain-preserving Newton globalization (GFL-RMS10 TS trial) | Profile-B Zf=0.1i died at t=3.25s before sg_trip; classified RMS10 trial-iterate lowVoltagePowerInversion throw aborted the whole Newton solve instead of rejecting the trial. Fix verified by targeted tests; dt=0.005 passes and reaches reclose; dt=0.01 still fails with domain_rejected_trials=0 (separate non-domain stall, tracked as IBR-2026-07-20-01) | [Domain-preserving Newton globalization](2026-07-19-domain-preserving-newton-globalization.md) |
| IBR-2026-07-20-01 | OPEN | dt=0.01 Newton/Jacobian stall at t=3.25s | After the domain-preserving fix, dt=0.01 still fails at t=3.25s with domain_rejected_trials=0 and subdivision_depth=4; non-smooth residual trajectory and near-singular rcond indicate a non-domain Newton/Jacobian failure (limiter discontinuity or conditioning), not a trial-voltage violation | [dt=0.01 Newton stall](2026-07-20-dt01-newton-stall-t325.md) |
| UI-2026-07-20-01 | OPEN | IBR Full result schema | Completed IEEE14 Full Analysis result lacks `r.ts.internal_substeps`, so its targeted test errors; reproduced identically at clean baseline `83390db` | [IBR Full internal-substeps schema](2026-07-20-ibr-full-internal-substeps-schema.md) |
| SWEEP-2026-07-21-01 | RESOLVED | GFL-RMS10 PLL phase-detector sign | The ~3.4e5 unstable mode was mis-classified as a device property. Root cause: `v_q = -imag(Vdq)` inverted the PLL phase-detector vs Yazdani eq 8.1 (`v_q=+imag`), giving det(J_PLL)<0 (saddle); analytic trace +3.36e5 matched the observed +3.37e5. Fixed to `v_q=+imag`; SSSA max_real +3.37e5 -> -11.23; equilibrium preserved; 6-gate oracle + 109/109 targeted tests pass (full regression omitted per user) | [GFL-RMS10 SMIB unstable mode / PLL sign](2026-07-21-gfl-rms10-smib-unstable-mode.md) |
| SWEEP-2026-07-21-02 | RESOLVED | SSSA load-sweep plots | Tracked plots treated adjacent load-point assignments as modal identities, omitted modes/load points, and lacked source-labelled dq/P/Q diagnostics | [SSSA load-sweep plot mapping](2026-07-21-sssa-load-sweep-plot-mapping.md) |
| SWEEP-2026-07-22-01 | RESOLVED_BY_REDUCED6_MODEL | GFL PLL tuning (zeta~43, no oscillatory mode) | GFL-RMS10 spectrum entirely real with a spurious `-3.4e5` PLL pole. Root cause: `kp_PLL=9.2/ts^2` (should be `9.2/ts` per Teodorescu eq 4.38) plus a spurious `omega_b` multiplier on the PI output, inflating loop gain ~3770x -> zeta~43 (intended 0.707). The reduced 6-state EECON49 `gfl_reduced6_model` uses the source-correct PLL (no omega_b, kp=1.20/ki=5.00) -> complex PLL pair `-0.58+/-j2.15` (0.341 Hz); 53/53 targeted pass | [GFL PLL tuning zeta43](2026-07-22-gfl-pll-tuning-zeta43.md) |

New records use `YYYY-MM-DD-short-slug.md` and contain: status, observed
symptom, deterministic reproduction, affected tree/environment, evidence-backed
root cause, falsified hypotheses, correction, verification, and limitations.
| SWITCH-2026-07-26-01 | RESOLVED | IEEE14 AGSI++ GFL/GFM switch study | Switching map declared voltage collapse from the intentional during-fault sag (and masked the switch outcome); `plot_results=false` still wrote 8 PNGs; a scheduled fault/step on an unknown bus was silently relocated to bus position 1. Corrected map tally 2/22/0 (was 2/16/6); targeted 69/69 | [IEEE14 switch map classification](2026-07-26-ieee14-switch-map-classification.md) |
| SWITCH-2026-08-03-01 | RESOLVED_TEST_CONTRACT | IEEE14 switching report and demo figures | Report omitted explicit hybrid-mode, per-device timer, and SG-reclose handback semantics; adding the required binary mode timeline exposed a stale exact-eight-figures assertion | [IEEE14 switching report mode semantics](2026-08-03-ieee14-switch-report-mode-semantics.md) |
| SWITCH-2026-08-03-02 | RESOLVED_DIAGNOSTIC_WORKFLOW | IEEE14 source-case restoration/full-state island dispatch | REGFM_B1/all-KCL route now completes the exact 160-s sequence and coordinated handback; diagnostic synchronizer/dispatch/governor assumptions and 1.000-s close-time sensitivity preclude a deployment claim | [IEEE14 source-case restoration](2026-08-03-ieee14-source-case-restoration.md) |
| SWITCH-2026-08-04-01 | RESOLVED_DIAGNOSTIC_160S | Bounded AGSI and publication-evidence correction | AGSI is bounded in [0,1]; the production all-KCL run reaches 160 s and returns all IBRs to GFL at accepted SG handback, while raw evidence and display-only ripple remain explicitly separated | [Bounded AGSI and publication evidence](2026-08-04-agsi-bounded-publication-evidence.md) |
| SWITCH-2026-08-04-02 | RESOLVED_TEST_CONTRACT | Mixed SG/IBR equilibrium test | Exact PF-port warm starts equalled solved `Tm/Efd`, but the test incorrectly required every solved input to differ from its initializer | [Exact warm-start test oracle](2026-08-04-exact-warm-start-test-oracle.md) |
| SWITCH-2026-08-04-03 | RESOLVED | Hybrid TS residual provenance | A successful subdivided step published the rejected parent residual without separately identifying the accepted-leaf residual | [Subdivision residual provenance](2026-08-04-subdivision-residual-provenance.md) |
| SWITCH-2026-08-04-04 | RESOLVED_FOR_THIS_RUN | MATLAB batch/cache provenance | Terminating a yielded shell cell left its MATLAB child alive, creating two writers for one 160-s cache; the obsolete PID pair was identified and stopped | [MATLAB batch orphan/cache race](2026-08-04-matlab-batch-orphan-cache-race.md) |
| SWITCH-2026-08-04-05 | RESOLVED | Mixed-resource EMF6 initialization | An SG-only network initializer, late frozen-state check, and incomplete exact `T'_{q0}=0` handling could invalidate or hide mixed-equilibrium inputs | [Mixed EMF6 initialization contract](2026-08-04-mixed-emf6-initialization-contract.md) |
| ET-FCS-2026-08-04-01 | RESOLVED | ET-FCSPS project-provider dispatch | Text provider names were passed to `func2str`, so the authenticated trial-table path failed closed before screening | [ET-FCSPS provider-name dispatch](2026-08-04-et-fcs-provider-name-dispatch.md) |
| ET-FCS-2026-08-04-02 | RESOLVED | ET-FCSPS production SG-trip adapter | Breaker-open was sent through the IBR transfer helper and orientation-sensitive identity comparison rejected equivalent candidate sets, so every authenticated production trial failed before prediction | [ET-FCSPS production SG-trip adapter](2026-08-04-et-fcs-production-trip-adapter.md) |
| ET-FCS-2026-08-05-01 | RESOLVED_DIAGNOSTIC_BOUNDARY | ET-FCSPS reference-PCC stress | Fault clearing one fixed step before SG loss completed the legacy arm but left no post-trip candidate inside the declared voltage screen; ET/BO correctly failed closed before prediction | [ET-FCSPS reference-PCC stress boundary](2026-08-05-et-fcs-stress-outside-validity.md) |
| DOC-2026-08-05-01 | RESOLVED | IEEE14 switching report text | Report stated an 18-state IBR superset, listed 11 of 13 GFM states, and claimed the GFM branch owns no PLL state; the production route is the 20-state dual model whose GFM branch carries a PLL pair | [Report IBR state count and PLL ownership](2026-08-05-report-ibr-state-count-pll.md) |
| PF-2026-08-05-01 | RESOLVED | eecon49 Q-limit enforcement | The case converter emitted a 10-column bus_data and padded Qmin/Qmax with ±Inf, and the switching builders disabled Q-limit enforcement, so the PV-to-PQ Q-limit switch was a silent no-op; limits were never registered from mpc.gen | [eecon49 Q-limits not registered](2026-08-05-eecon49-qlimits-not-registered.md) |
| TS-2026-08-05-01 | RESOLVED | eecon49 fault-on solver lease | A 200-s run on the Phase D operating point failed closed at the 85-s fault-on right-limit: ts_algebraic_solve exhausted its fixed 30-iteration cap at residual 1.116e-06 vs kcl_tol=1e-06 (line_search_failures=0 = converging). Lease raised 30->60 (NUMERICAL_METHOD), kcl_tol unchanged; 200-s run now passes full | [eecon49 fault-on solver lease](2026-08-05-eecon49-faulton-solver-lease.md) |
| SWITCH-2026-08-10-01 | RESOLVED | Two-term AGSI handback and EECON49 PQ reference | Two-term per-device severity handback reaches all-GFL at 146.400 s in the 200-s run; SG reclose remains a separate GFM-preserving transaction; targeted checkpoint 44/44 PASS | [Two-term AGSI handback and healthy-PQ reference](2026-08-10-agsi-severity-handback-q-reference.md) |
| SWITCH-2026-08-10-03 | IMPLEMENTED_PENDING_250S_EVIDENCE | SG reclose C1 handback and SG-online SSSA gauge | Reclose reset the complete input vector by 1.28758 pu and SG-online SSSA used a rigid common-angle coordinate as its dominant pole; input continuity, C1 transfer, SG-owned gauge quotient, prospective close audit, FD robustness and staged authenticated release are implemented | [SG reclose command step and angle gauge](2026-08-10-sg-reclose-command-step-and-angle-gauge.md) |
| SWITCH-2026-08-10-02 | IMPLEMENTED_PENDING_200S_GATE | Production GFL/GFM state ownership | Source-case production used GFM-with-PLL and GFL-without-PLL; new shared-plant route assigns PLL only to GFL, VSG without PLL to GFM, and a project-derived active DC-energy closure | [Production GFL/GFM state ownership](2026-08-10-eecon49-production-state-ownership.md) |
| TEST-2026-08-11-01 | OPEN | Shared algebraic solver test contract | test_algebraic_residual_in_tol_range hardcodes the retired 30-iteration lease; with the approved 30->60 lease the halving residual reaches 2^-60 = 8.67e-19 < tol, so the solver correctly converges and the expected ts_algebraic_solve:failed never throws. Pre-existing and unrelated to the TS performance work that found it | [Stale algebraic lease test](2026-08-11-stale-algebraic-lease-test.md) |
| PERF-2026-08-11-01 | RESOLVED | Switched-TS kernel runtime | A 200-s production run took 2-3 h. Measured causes: 2,532,277 `makeValidName` rebuilds of a constant field name (~38%), device struct-array copies (~19%), and 82 FD residual evaluations per Jacobian. S1 (constant-key threading, flat dispatch tables) and S2 (structurally derived FD column grouping, 82->41) are bit-identical (compressed-arm AbsTol 0 + whole-arm fd_structure_check + 102/103 targeted); grouped vs per-column measured 1.72x in-session and the full 200-s rerun dropped to 20.3 min. The existing cache is a stale pre-fix diagnostic-variant artifact and is not a valid equivalence oracle. FD-step scaling, limiter non-smoothness, an extrapolating predictor and subdivision-hint hysteresis were each falsified by measurement and reverted | [TS kernel runtime optimization](2026-08-11-ts-kernel-runtime-optimization.md) |
| ADAPT-2026-08-12-01 | RESOLVED_EVIDENCE_CORRECTED | Adaptive-hybrid TS discontinuity restart | Resetting `dt_adaptive=dt_min` at each event remains the implemented discontinuity-restart fix. A later forwarding audit proved that all caller overrides except `stepper` were silently dropped before ADAPT-2026-08-13-02; therefore the historical exact counts (`3402` samples, `235` rejects, `dt_max=0.5`, ~410 s) are stale as override-specific evidence and require current-tree revalidation. Fresh post-forwarding LTE/rollback/landing/bit-identity gates pass 11/11; Dv=20 chronology remains separately blocked | [Adaptive-hybrid discontinuity restart](2026-08-12-adaptive-hybrid-discontinuity-restart.md) |
| ADAPT-2026-08-13-02 | RESOLVED | Adaptive option forwarding cell orientation | `run_hybrid_case` transposed a row cell list before a MATLAB `for` loop, producing one column and forwarding only `afield{1}='stepper'`; all other adaptive overrides were silently dropped. Removing the transpose restores per-option forwarding. A `reject_limit=0` fail-closed oracle plus LTE/rollback/landing/bit-identity gates pass 11/11; old override-dependent counts are explicitly stale | [Adaptive option forwarding](2026-08-13-adaptive-option-forwarding-cell-orientation.md) |
| RECLOSE-2026-08-12-01 | RESOLVED | SG reclose synchronizer command floor | Production SG reclose `SYNC_TIMEOUT`: SG locked FREQUENCY (df->7e-8) but its angle FROZE ~106 deg from grid, dwell never reached, `handback_status=NOT_STARTED`. The offline synchronizer command was floored at `Pmin=0`; when the SG leads the grid the proportional angle term demands a NEGATIVE (decelerating) command that clips to 0, and since model damping `D*omega` also vanishes at omega=0 (deviation) the rotor coasts to grid speed at a frozen angle offset. Command trace = 0 for all 2202 samples confirms the clip. NOT the handback SSSA gate (4-GFM is stable, omega=-0.23) and NOT a d63f48d->HEAD regression (byte-identical; the 154.3 success was a stale-cache misreport). Fix: symmetric authority `Pmin=-Pmax` for the breaker-open synchronizer actuator (PROJECT_DERIVED; online governor keeps Pmin=0). Restores the critically-damped 2nd-order loop `e_theta''+2*zeta*wn*e_theta'+wn^2*e_theta=0`. Proven by ODE oracle, unit test 3/3, and arm reclose SUCCESS@15.5s; production 170s confirmation pending | [SG offline synchronizer Pmin floor](2026-08-12-sg-offline-synchronizer-pmin-floor.md) |
| TD-2026-08-12-01 | RESOLVED_EVIDENCE_CORRECTED | EECON49 command-delay placeholder instability | Placeholder `T_d=0.02 s` command-delay states produced a spurious all-GFL SG-online SSSA instability (`+1.29`@11.4 Hz). Physical `T_d=1.5/f_sw=0.3 ms` is unresolvable by the RMS step, so singular perturbation gives `v_del=v_cmd`; dual 20->16 and standalone 12->10. Equilibrium/retained dynamics and all-GFL stable result (`-0.065`) are verified. The old statement that every `dt<=0.05` completed the production chronology is superseded: Dv20 canonical chronology is blocked by TS-2026-08-13-03, and no 250-s run is claimed | [EECON49 command-delay reduction](2026-08-12-eecon49-command-delay-reduction.md) |
| RECLOSE-2026-08-13-01 | RESOLVED_REACHABILITY / RECLOSE_CHRONOLOGY_BLOCKED | Islanded full-state VSG reachability | Historical Dv=1.50 gives 66.7 % droop and cannot ride through the 1.3319-pu SG-loss deficit. Its earlier `SOURCE_DEFINED` label was a cross-family citation error. Owner-set PROJECT_DERIVED Dv=20 uses an independently frozen 5 % droop/damping design basis and restores the deficit envelope, while M=0.08 remains unchanged. Reclose still is not reached because the canonical chronology hits TS-2026-08-13-03 before `sg_on` | [Islanded VSG reachability](2026-08-13-islanded-vsg-inertia-reclose-unreachable.md) |
| TS-2026-08-13-03 | OPEN | Dv20 post-line nonsmooth Newton wall | The authenticated all-four arm passes the bolted fault and commits line trip, but its trajectory crosses the circular raw-current-reference/conditional anti-windup active set; worst residual is `IBR3.gfm_xi_Vd`, coupled `rcond(J)` falls to about 2.6e-6, and line search exhausts. Crossed state/topology probes prove line right-limit KCL is feasible and the identical state solves at h<=1e-3. No equation/gate/tolerance change is authorized; report reclose as not reached | [Dv20 post-line Newton wall](2026-08-13-dv20-post-line-nonsmooth-newton-wall.md) |
| MODEL-2026-08-13-01 | DELIVERED | Decoupled GFM swing + reference-AGSI overlay + provenance correction | New project-owned GFM (`nx=11` standalone, `nx=17` dual) separates droop `R`, washout transient damping `D_t/wD` and inertia `M`; the washout state is mathematically unavoidable because two proportional terms collapse to one coefficient. EECON49 baseline kept byte-identical (SHA-256 verified). Measuring `K` from the full-KCL Schur SSSA gives `0.1135..0.1862 pu/rad`, 27--44x below the unmeasured `K~=5` estimate, so three published damping figures are WITHDRAWN: at 5 % droop the coupled VSG is over-damped (`zeta=4.22..5.40`, not `0.81`), `Dv=1.50` gives `0.41` (not `0.06`), and `M=5.0` gives `0.68` (not `0.10`), which also voids the earlier reason for rejecting `H_v=2.5 s`. Decoupled production defaults are `R=0.05, D_t=0, wD=50` -- the first shipped pair `wD=3.0, D_t=20` was withdrawn the same day by MODEL-2026-08-13-02 after it destabilised the all-GFM island. Also corrects the false "source PDF is password-protected" claim (`pdfinfo`: not encrypted; page 5 readable) -- it was a Read-tool limitation, so `Dv=1.50` is `SOURCE_PRINTED`. Opt-in `agsi_reference` publishes `J_R/J_P/J_SCR/J_lock` in-band flags as post-processing with no aggregate index; switching still consumes `J_V/J_f` only, proven byte-identical. 24/24 new tests, 163/163 targeted batch A, no gate or threshold changed. Does NOT resolve TS-2026-08-13-03 | [Decoupled GFM model + AGSI reference](2026-08-13-decoupled-gfm-model-and-agsi-reference.md) |
| MODEL-2026-08-13-02 | RESOLVED | Decoupled washout corner destabilised the all-GFM island | The first decoupled production values (`wD=3.0, D_t=20`) made the authenticated all-four SG-off island small-signal UNSTABLE (`Omega=+0.336` vs the coupled baseline's `-0.483`), so the SG-trip transaction refused to commit and the chronology failed closed at `t=1.0` with `candidateNotReady`. Cause is filter placement: `wD=3.0 rad/s = 0.48 Hz` sat on the island's own swing mode (0.623 Hz, 3.92 rad/s), all four washout poles migrated into it, and the phase lag turned the damping path into positive feedback. The basis was wrong because `wD` was sized from the SG-ONLINE `wn=23..30 rad/s`; the ISLAND coefficients measured through the production dispatch path are `[-0.0151 0.0015 0.1169 0.0525] pu/rad` — two of them `<=0`, so the single-machine cubic has no real `wn` there at all. The measured `(wD x D_t)` surface then showed NO `D_t>0` improves this system at any `wD` from 3 to 100, while SG-online margins move `1.1e-6` over the same sweep. Implementation falsified as a cause: at `D_t=0` the island margin matches the baseline to `1.2e-13`. Correction: `D_t=0` (measured result, not an unset default) and `wD=50` (REGFM_B1 Table-1 SOURCE_VERBATIM, ~13x above the island mode). The "5 % droop AND zeta=1/sqrt(2)" claim is withdrawn as a single-machine figure given system scope; its test was rewritten to assert only the structural separation. 25/25 tests, incl. a new island oracle that keeps the withdrawn pair refused | [Decoupled washout corner island instability](2026-08-13-decoupled-washout-corner-island-instability.md) |
| TEST-2026-08-13-04 | OPEN | Standalone EMF6 SSSA oracle equilibrium | `stability.synchronous_emf6_ssa` refuses to build on the default `mission` profile: its own equilibrium residual is `4.485e-01`, so `test_sg_stationary_initializer_matches_emf6_oracle` errors and that independent cross-check is unavailable. Reproduced identically on a pristine `e233b6c` worktree, so it is pre-existing and unrelated to the decoupled-GFM work that found it. The production mixed path solves the same network to `2.02e-13` in the same test file. Root cause not claimed; the standalone route is SG-only, which is the hazard already recorded as SWITCH-2026-08-04-05. Do not relax the oracle tolerance or delete the assertion | [Standalone EMF6 oracle equilibrium](2026-08-13-standalone-emf6-oracle-equilibrium.md) |
| AGSI-2026-08-14-01 | OPEN | All-4-GFM commit carries IBR2 stale voltage-loop integrators | Controlled replay at the `t=53.4025` augment (identical devices, network, event context, inputs, solver; only the initial state differs) shows the committed configuration is STABLE and the ARRIVING STATE is not: production state SLIPS (relative-angle excursion 4358 deg) while the certified equilibrium under the SAME installed inputs stays synchronised (6.6 deg). Group-by-group injection isolates the carrier to TWO numbers: `IBR2.xi_Vd` (-0.055 from certified) and `IBR2.xi_Vq` (+0.029). Injecting only `xi_Vd,xi_Vq` reproduces the slip (4332.8 deg) and injecting only IBR2's voltage loop gives 4827.4 deg, while IBR3/IBR6/IBR8 voltage loops give 13.0/12.2/7.4 deg. Starting from the FULL production state and re-seeding only those two coordinates yields 8.6 deg SYNCHRONISED; re-seeding `gfm_E` instead leaves 4328.9 deg. Cause: IBR3/IBR8 transfer from GFL at this transaction so `equilibrium_initialize` conditions their voltage loop (`E-|V|=0.00000`), but IBR2 has been GFM since `t=20`, passes through no transfer, and enters the new certified operating point carrying 33 s of integrator history from the previous one- and two-GFM configurations (`E-|V|=+0.00991`, the only nonzero one). Two intermediate claims from this same investigation are WITHDRAWN in the record: '96 % of the deviation is angular therefore angles carry it' (raw -23 rad angles injected alone stay synchronised at 8.6 deg) and a '25-40 % basin' figure (its alpha-ladder scaled 2*pi-periodic angles linearly, so intermediate points are not physical interpolants). Nine hypotheses falsified by measurement: dispatch mismatch (installed input matches the certificate to 0.2 %), saturation at commit (`sat=0 hold=[0 0]` on all four), `omega` mis-seeding (transfer carries `omega_PLL_pu`; 0.9829..0.9836 coherent), `K_IBR2<0` (negative everywhere yet the certified equilibrium rides 2.0 pu), reference choice (`ref=IBR2` equals `ref=IBR6`), inertia (`M=2.5` does not help), fewer formers (2-GFM slips where all-4 rides; `[3 4 5]` has NO equilibrium), the +20 % load step (15.8 vs 17.1 deg), and the single-GFM window (`omega=1.000000` flat). No parameter, gate, or contract changed. CORRECTION ATTEMPT 1 (incumbent `xi_Vd/xi_Vq` conditioning from the authenticated certificate, plan AGSI-2026-08-14-01) is implemented with 50/50 contract tests passing, but its full-chronology gate FAILS EARLIER (adaptiveDtMin at t=24.9 s vs baseline 152.0 s): determinism proves the only divergence is the exact xi assignment, replay proves the t=24.06 all-four commit is outside the basin WITH or WITHOUT conditioning, and the conditioning moved the supervisor path so the all-four commit now lands mid-transient (arriving voltage-profile distance 0.064 pu vs 0.020 pu at the settled baseline commit). Correction kept uncommitted; the defect stays OPEN pending an arriving-state admissibility / supervisor-interlock plan | [All-4-GFM commit outside basin](2026-08-14-all4-gfm-commit-outside-basin.md) |
| RECLOSE-2026-08-15-01 | RESOLVED | Post-reclose handback command timescale | First run to reach SG reclose on the IEEE14 EECON49 chronology (SUCCESS t=159.3436, all three synchronism sub-gates positive) then died at t=173.006 with `adaptiveDtMin`. `derive_handback_duration` returns ONE duration `T=max(T_minimum_hold,t_mode,t_control)` and `enter_online_governor` used it for EVERY post-reclose command, so the SG field voltage walked 0.169925->0.989169 over `t_mode`=13.875 s, which is 11.6x the declared actuator response `t_control=-log(rho)*max([Tsv Tch TA])`=1.198 s. The SG stayed under-excited (Q_SG -1.1245 vs -0.1957 pu at equilibrium), IBR2's voltage-loop q integrator overshot 5.3x (xi_Vq -0.2114 vs -0.0396), |I_ref| hit Imax=1.2, `conditional_hold` froze both integrators to six figures for 12 s, and the coupled algebraic system lost solvability. Not a solver artefact: four arms (baseline, `fd_perturbation='scaled'`, `reject_limit=40`, fixed dt=0.02) all die at the same place, and two ramp durations (15.439/13.875 s) die at the same ramp fraction alpha=0.9993. FIX: split the timescales opt-in - `derive_handback_duration` also returns `t_control`, `enter_online_governor` takes an optional `T_efd`, and only the FIELD-VOLTAGE command uses it; mechanical power and IBR references keep the full duration so SWITCH-2026-08-10-03's 1.28758 pu command-step concern is unaffected. New option `handback_efd_timescale` in {mode (default, byte-identical), control}. VERIFIED: 250 s completes, reclose SUCCESS, f_coi=60.000000 Hz, terminal state reproduces the published EECON49 figure (SG1 134.62 MVA, IBR 36.31/33.02/50.86/30.64 MVA) and IBR2 |I|=0.36623 matches its certified equilibrium 0.366232, 69% below the limit it had been pinned on. Falsified en route: inner-loop windup (xi_Id ~1e-6), a large angle swing (a rad-vs-deg unit error of mine; true pairwise gap ~10 deg), stale E_ref (equals |V| at the destination to 0.000e+00), and the `handback_complete` gate as primary cause. Also found and fixed in the scratch driver: passing `s.scenario_opt` as the `scenario` argument makes `ibr_config_selector` resolve no dispatch, so every SG_ON row was certified at IBR P_ref=0 with the SG at 2.55-2.73 pu; production `run_hybrid_case` passes the full struct and is unaffected | [Post-reclose handback command timescale](2026-08-15-post-reclose-handback-current-limit-wall.md) |
| AGSI-2026-08-16-01 | RESOLVED | Reference-AGSI overlay absent from the delivered chronology | The switching-decision figure needs `S`, `J_V`, `J_f`, `J_R`, `J_P`, `J_lock`, `J_SCR`, and NONE of them is in `output/diagnostics/ieee14_eecon49_chronology.mat`: the overlay option is `agsi_reference`, it defaults FALSE (`ts_simulate_ibr_hybrid.m:1473`), its call site is gated on that flag (`:1339`), and the published driver never sets it. Post-processing cannot recover the terms because the admittance log the Thevenin/SCR computation needs is built only under the same flag (`:80`, `:703`); the similarly named published field `res.Y_log` is `samples.topology`, i.e. topology LABELS, not matrices (`:1240`). Fix touches no production code: the new comparison runner enables the option and writes to a separate artifact, so the delivered file still reproduces byte-for-byte from the driver the README cites. The re-run was gated on bit-identity, not on the existing inertness test alone, and passed exactly (`max|dx|=max|dy|=max|du|=0`, identical `t`/`sample_side`/modes/event log, reclose 159.343560, overlay `status=OK`, COI residual `0.000e+00` over 4994 samples, `J_SCR` finite at every sample for all four devices). Also falsified: `support_supervision_status` is assigned at `:1288` but dropped by the `copy_fields` whitelist in `run_hybrid_case.m:517-535` (zero occurrences in either artifact); `severity_gamma_on/off` are not republished in `result.metadata`; and `J_f`/`J_R` are system quantities with no device dependence (`agsi_reference_terms.m:190,193`), so four coincident traces would misrepresent them | [AGSI overlay absent from the delivered chronology](2026-08-16-agsi-overlay-absent-from-delivered-chronology.md) |
| DOC-2026-08-17-01 | RESOLVED | Report described a 20-state IBR the run does not integrate | Both switching reports stated a fixed 20-vector IBR state (`x_pl(3) | GFL 4..11 | GFM 12..20`), `A_GFL={1:11}`, `A_GFM={1:3,12:20}`, an `11->12` active-dimension step, and two integrated command-delay rows per branch. The executed model is `+ibr/eecon49_dual_mode_model.m` with `dev.nx=16` (`:117`), layout `plant 1:3 | GFL 4:9 | GFM 10:16` (`:10-16`) and `gfl_active=1:9`, `gfm_active=[1:3 10:16]` (`:78-79`), reached via `case_profile='eecon49_figure4'` -> `ibr_model_id='eecon49_dual'` (`+cases/scenario_ieee14_1sg_4ibr.m:98-103`). Decisive independent check: the delivered trajectory has `size(x_traj,1)=70 = 6 + 4*16`; a 20-state IBR gives 86 and the 17-state `decoupled_dual` gives 74. Cause: the report body predates `TD-2026-08-12-01`, which removed the delay states by singular perturbation (`T_d=1.5/f_sw~=0.3 ms << dt`) and was never propagated to the text. Documentation-only: every retained-state equation, and therefore every reported number, figure and gate, is unaffected. FIXED in `report_ieee14_switch_en_rev2.tex` (16-state layout, `9->10` step, new `sec:ibr-reduction` deriving the reduction, Limitations bullet disclosing the correction). FIXED 2026-08-22 in `report_ieee14_switch_th_rev2.tex` as well: 16-state vector, `A_GFL={1:9}`/`A_GFM={1:3,10:16}` with `9`/`10` totals, 16-row state table with GFM renumbered `10..16`, GFL block `(4)-(9)` and GFM block `(10)-(16)` with both per-state proofs renumbered and the delay sentences removed, the composite active-set list, the `n_x` identity (`5+4*9=41`, `5+4*10=45`), and a new `sec:ibr-reduction` deriving the reduction. The blocker is resolved rather than bypassed: the equation renumbering is APPLIED and MAPPED (52->48 numbered equations; (1)-(32) unchanged; (33),(34) and (42),(43) deleted as the four command-delay rows; (35)-(41)->(33)-(39); (44)-(52)->(40)-(48), i.e. eq:transfer 44->40 ... eq:nxcount 52->48), the map is recorded in the report header so it travels with the file, and NO equation was added because every new derivation is inline math. Active dimensions verified against the delivered artefact rather than asserted from source: the regenerated `n_x` figure reports `nx_before_trip=41` with per-device `[5 9 9 9 9] -> [5 10 9 9 9]`. Thai PDF rebuilt: `xelatex` twice, exit 0, no undefined references, 32 pages, highest printed equation number 48 | [Report IBR state count stale](2026-08-17-report-ibr-state-count-stale.md) |
| NUM-2026-08-20-01 | RESOLVED | EECON49 IBR DC-link closure was an ideal source | The owner questioned a small-signal participation of exactly `1.000` on a DC-link voltage, four times over. Measured on the delivered 250 s adaptive trajectory (4994 accepted samples): `V_dc` = 1.000000000000 pu for every sample of all four converters, `range = 0.000e+00` exactly. Cause: the closure `Idc = Pac/vdc + (C/Tdc)(Vdc0-vdc)` with `dv=(Idc-Pac/vdc)/C` cancels `Pac/vdc` identically, leaving `dVdc/dt=(Vdc0-Vdc)/Tdc`, whose equilibrium is the initialiser's own `x(3)=Vdc_ref`; and `C` cancels too, so the case value `Cdc=0.10` never entered any residual, Jacobian or eigenvalue. Hence `-1/Tdc=-10` with algebraic multiplicity four and, because the unit vector on that coordinate was exactly a LEFT eigenvector, a participation of exactly 1.000 as algebra rather than rounding. FIX (owner-authorised, `PROJECT_DERIVED`): `Idc=(Edc-Vdc)/Rdc` with a linear-region overvoltage chopper, in one shared helper (`+ibr/dc_source_thevenin_{params,rhs}.m`) so GFL and GFM cannot drift apart; `Edc` is FORCED by requiring the dispatched point to be an equilibrium, `Rdc` follows from a declared regulation `eps_dc=0.10` bounded above by `eps<~0.115` from the requirement that the link support the largest current-limited power at margin 2. State count, order, layout, active sets, transfer contract and schema unchanged (still 70 rows). One kernel change: `ibr:dc_source_thevenin:dcVoltage` registered in `ts_step_composite.m` `trial_domain_classifier` so a line-search trial proposing `V_dc<=0` is a rejected trial, not an abort - a violation at an ACCEPTED state still aborts, so no gate is relaxed. VERIFIED by a prediction made BEFORE recomputation: `lambda_dc=-100+10*Pac/Vdc^2` predicts four distinct real modes just above -100; measured -96.812311/-92.911157/-92.693034/-91.111042 with `V_dc` dominant, and inverting on them returns per-converter powers 0.319/0.709/0.731/0.889 pu matching the time-domain loading. Equilibrium `V_dc` is now solved (0.989226..1.002153 pu, per-configuration spread 3.7e-4..1.08e-2) instead of pinned at 1.000000. `dVdc(0)=-1.06e-14` so the power flow is untouched; admissible SG-off count unchanged at 7 of 15; flagship result survives (adaptive 250 s, reclose SUCCESS 159.2397 vs 159.3436), wall time 882->1307 s. FALSIFIED: the primary agent's own claim that the fault would over-volt the link (a Thevenin source is bounded by its EMF, `V_dc<=Edc=1.0453<Vdc_max=1.10`, so the chopper is provably inactive and is reported as such); and enlarging `eps_dc` to make the mode step-resolvable, rejected because it would choose physics from numerical convenience. New `tests/test_ibr_dc_source_thevenin.m` 9/9 including an eigenvalue oracle by central difference and a test that halving `C_dc` doubles `dVdc/dt`, which the retired closure could not pass. Limitation stated in the report: `\|lambda\|*dt~4.8`, so the DC mode is under-resolved at the nominal 0.05 s step and a firmer source belongs to EMT. CORRECTED 2026-08-22: the record originally claimed all five 250 s arms had been re-run; disk showed two complete, one truncated by a kill and two never started, and no `summary.mat`. The closure itself stands — an independent `h5read` of the delivered cache gives `V_dc` ranges 7.0e-02 to 8.8e-02 pu against exactly 0 before, with every maximum below `Edc=1.0453` so the chopper provably never conducted. Truncated arm renamed not deleted; the three outstanding arms were resumed with `reuse_completed=true` and `summary.mat` now carries five rows with every expectation MET. `pinned_gfm4` still stops at `adaptiveDtMin` t=25.49 s, proven PRE-EXISTING (ideal 25.4905 vs non-ideal 25.4907, a 2e-4 s shift), and `locked_gfl` is unchanged in every field because it refuses before any Newton solve. Largest change is `pinned_gfm2`, reclose 3.8021 s earlier, ~37x the adaptive arm's shift. English report figures and scalars regenerated from the non-ideal cache, closing its mixed provenance IN SOURCE ONLY -- the delivered PDF is still the 08-20 build and still embeds the ideal-DC plots, because rebuilding needs `newtxtext.sty` which is absent on the Linux host and the font contract forbids substituting a package to force a compile; a failed `pdflatex` pass DELETES the PDF, restored byte-identical from a pre-run copy. Thai report brought fully in line 2026-08-22 under DOC-2026-08-17-01 and DOC-2026-08-22-02, including its rebuilt PDF; the report PROSE that still defended the retired cancellation in BOTH reports is recorded separately as DOC-2026-08-22-02, because the 08-20 revision updated the displayed DC row but not the paragraph that proves it | [Ideal DC link carried no dynamics](2026-08-20-ideal-dc-link-carried-no-dynamics.md) |
| DOC-2026-08-22-01 | RESOLVED | Report-scalar generator stamped an unearned bit-identity claim | `generate_ieee14_report_scalars.m:52-55` hardcoded "That re-run was gated on being bit-identical to the delivered artifact" into every file it wrote. True only of the ideal-DC cache (`AGSI-2026-08-16-01`); stamped onto the `_dcreal` cache it is false, because `NUM-2026-08-20-01` changed the DC closure on purpose (4990 samples vs 4994, reclose 159.2397 vs 159.3436). The claim is a property of one cache while the code treated it as a property of the generator, and `result_file` is an exposed option, so any future cache inherited it silently. Second instance: the `Regenerate with:` line printed a bare call that defaults back to the ideal cache, so following the file's own instructions would have overwritten the non-ideal scalars with ideal ones and raised no error. Fixed by selecting the sentence from the cache actually read, using an exact `fileparts`+`strcmp` component comparison rather than `contains`, which would have matched `..._dcreal` too and reintroduced the defect; the non-ideal branch states bit-identity is NOT claimed and why. `checkcode` clean, macro values unchanged, ideal branch preserved verbatim | [Hardcoded bit-identity provenance](2026-08-22-hardcoded-bit-identity-provenance.md) |
| DOC-2026-08-22-02 | RESOLVED | Report DC-link prose contradicted its own DC-link equation | `NUM-2026-08-20-01` updated the displayed DC-link row of both switching reports but not the prose that proves it, so the English `rev2` report contradicted itself inside one subsection: the align row printed the Thevenin-plus-chopper closure while the "Proof of each row" paragraph four lines below still said "the coded current is `I_dc=P_ac/V_dc+(C/T_dc)(V_dc^0-V_dc)`", that the feed-forward "cancels exactly", that "this cancellation is deliberate", and that "at steady state row (3) gives `V_dc=V_dc^0`" -- the exact three claims that defect was opened to remove. Five more in the same sweep: the English glossary listed `T_dc` as a live parameter; the English figure-source paragraph AND file header declared the IDEAL cache `ieee14_gfm_lock_compare/adaptive_250s.mat` after every figure had been regenerated from `..._dcreal`, while the generated `run_summary_v2.tex` already declared `_dcreal`, so one page asserted two provenances; the English text promised "the figure of `n_x(t)` below" which the report does not contain (11 `fig:` labels, none a state-dimension figure); the Thai small-signal prose attributed "the four real roots near `-10 s^-1`" to the DC family, that value being `-1/T_dc` of the retired closure while the generated table it introduces prints -96.812311/-92.911157/-92.693034/-91.111042; and the Thai report `\input` a compact modal table that has NEVER existed plus a superseded generator-less `comparison_arms.png`, both inside `\IfFileExists` guards so the missing table was SILENTLY OMITTED instead of failing the build. Cause: proof paragraphs, glossary, provenance paragraph and header comment are prose blocks no generator owns, and LaTeX cross-checks nothing between an align row and the sentence under it. Fixed by deriving the coded closure in the proof (nothing cancels; `P_ac` drives and `C_dc` scales, so both enter residual and Jacobian; `dot V_dc=0` gives `V_dc^2-E_dc V_dc+R_dc P_ac=0` whose upper root is `V_dc^0` only at the dispatched loading, which is what fixes `E_dc`), correcting both provenance declarations to `_dcreal`, replacing the Thai `-10` sentence with the predict-then-measure argument, pointing the Thai report at the generated `sssa_modes_n1.tex` the English report already uses, and replacing `comparison_arms.png` with `comparison_full.png`+`comparison_windows.png` with the one cross-reference repointed. `write_sssa_modes_compact` was NOT repaired: it projects five columns and the current table has four, so pointing at the generated table is the smaller change and makes bit-identity trivially true rather than asserted. English label order and equation count unchanged (40 labels, 38+5 environments) so nothing renumbered there. Falsified: the primary agent's own earlier claim that only the Thai report was stale on the model. LIMITATION: the English PDF is still the 08-20 build because `newtxtext.sty` is absent on this host and the font contract forbids substituting a package | [Report DC prose contradicted its equation](2026-08-22-report-dc-proof-contradicted-its-equation.md) |
| GATE-2026-08-25-01 | RESOLVED | Admissibility gate compared the wrong quantity to its own declared basis | The owner asked why one converter never returns to GFL after the SG recloses. IBR2 was promoted at the `sg_trip` transaction (t=20), the SG reclosed SUCCESS at 159.2519, and IBR2 was still GFM at t=250 with `reselection_status=NO_FEASIBLE_SG_ON_ONE_STEP`. The mechanism was NOT missing: reconstructing the severity evidence from the delivered cache, IBR2's two-term AGSI stayed <=0.0072 for 1537 accepted samples over ~77 s, continuously below `gamma_off=0.35`, so the `T_d_off=1.0 s` dwell was satisfied repeatedly and the supervisor attempted the release on every one of those samples. With one committed GFM the only one-step target is `setdiff([2],2)=[]`, i.e. the all-GFL row, which carried `ready_to_commit=0` with `omega=-0.0651964`, `margin=-0.0348`, `insufficientRobustMargin`. ROOT CAUSE: the case contract declares the criterion as `5% damping at the 1 Hz electromechanical mode`, i.e. a damping RATIO `zeta>=0.05`, but `ibr_candidate_evaluate.m:401-402` applied an ABSOLUTE decay-rate floor `max(Re lambda)<=-gamma_req` to every root. The two coincide at exactly one frequency, `f*=gamma/(2*pi*zeta)=0.3183 Hz`, and diverge on both sides; at the blocking mode's `f=0.0198 Hz` the rate floor demands `zeta>=0.712`, 14x the declared basis. The blocking root `-0.065196+/-0.124395j` has `zeta=0.4642`, and the mode that actually limits the ratio is a DIFFERENT root, `-1.146045+/-7.254946j` at `f=1.1547 Hz` with `zeta=0.1560` -- the very electromechanical mode the derivation names, at 3.1x the requirement. The consequence was inverted: the configuration the supervisor refused to LEAVE (`sg_on sel=[2]`, `zeta_worst=0.1316`) is LESS damped in ratio than the one it refused to ENTER (all-GFL, 0.1560), and four admissible rows had narrower ratio headroom than the rejected row. FALSIFIED en route: that the 17-state DC extension created the mode (the algebraic-source limit and the pre-extension cached table both give `-0.065196` to six figures, and +20% load gives `-0.072055`, still short); that a runtime guard forbids zero GFM while an SG is online (`per_island_vf_check.m:77` counts `synchronous` as voltage-forming, `ibr_selector_table.m:131` states "SG_ON : zero GFM permitted", and `coordinated_sg_handback` already terminates in that exact state); and that the C1 ramp never finished (it completed at 173.127 exactly = 159.2519+13.8752). FIX (owner-selected after being shown the measured impact of three alternatives): the gate becomes `all(fd_omegas<0) && all(fd_zeta_worsts>=zeta_min)` evaluated at every member of the frozen FD set, with `zeta_min_damping=0.05` added to the case selector contract and the ratio minimised over the WHOLE spectrum because the worst-ratio root is generally not the rightmost one; a root at the origin maps to `-Inf` so a marginal mode can never certify. `omega` and `margin` are deliberately UNCHANGED because `margin` is the 4th sort key in `candidate_order_matrix.m:90-94` and ranking on the ratio instead would move the SG_OFF winner from `[2]` to `[5]` (measured) -- so a candidate may be admissible with `zeta_margin>=0` while `margin<0`. A SECOND defect had to be fixed for the first to have ANY effect: `validate_runtime_candidate_compatibility.m` compared index sets raw, and the stored zero-GFM row holds a literal `[]` (0x0) while `choose_sg_online_one_step` derives `1x0` via `setdiff`, and `isequal([],zeros(1,0))` is false in MATLAB, so the row matched the reshaping search loop but was then rejected as `manualCandidateNotInTable`; index sets are now canonicalised in SHAPE only, preserving exact membership and cardinality. VERIFIED: across all 23 evaluated rows exactly one verdict changes -- `sg_off` is identical row for row (its 8 rejections carry no spectrum and fail at the EQUILIBRIUM stage, so the non-monotone admissibility result is untouched) and `sg_on` goes 15->16 feasible. Narrowest ratio headroom is 1.4x the floor, so no row is marginal. Targeted: `test_ibr_selector_scr_sssa` 16/16 (13 existing + 3 new falsification tests with implementation-independent oracles), `test_ibr_selector_table_unit` 44/44, `test_ieee14_ibr_sg_on_integration` 12/12, `test_ibr_index_selected_gfm_commit` 13/13, `test_ieee14_ibr_sg_reclose_workflow` 20/20, `test_ieee14_ibr_ts_event_runner` 19/19. `tests/test_ibr_selector_scr_sssa.m:215` asserted `sssa_pass == (omega<=-gamma_req)` and was CORRECTED, not deleted: that equivalence is false under the declared contract and passed only because the sampled row happened to satisfy both, making it a latent trap. One new test of mine initially failed at `AbsTol 1e-9` by comparing `zeta_worst` (worst over three FD epsilons) against the `eps=1.0` spectrum alone, differing by 5.5e-08; the TEST was wrong and was corrected to use `fd_zeta_worsts(2)` as the exact oracle. REPORTED not hidden: the handed-back all-GFL state is better damped in ratio but SLOWER in absolute decay (`tau=15.3 s` vs `4.6 s`), so a hold derived from it would be 45.9 s rather than ~30 s. DELIVERED: five-arm set re-run under the corrected criterion (`ieee14_gfm_lock_compare_zeta`), then the release was still blocked and two MORE latent defects were found and fixed (-04 fingerprint payload, -05 accumulator), after which IBR2 returns to GFL at t=173.1271 with `max|dx|=max|dy|=0` over the shared pre-release window; reports EN (39 pp) and TH (26 pp) rebuilt with the new narrative, figure and macros | [Admissibility gate rate vs ratio](2026-08-25-admissibility-gate-rate-vs-ratio.md) |
| GATE-2026-08-25-02 | RESOLVED | Decoupled family broken by the 17-state DC source | `tests/test_ieee14_decoupled_full_state.m` fails 5/6 with `ibr:decoupled_dual_mode_model:branchLayout` "Expected a 10-state GFL adapter and an 11-state decoupled GFM adapter", raised at `+ibr/decoupled_dual_mode_model.m:71` via `build_mixed_resource_devices:148`. `+cases/scenario_ieee14_1sg_4ibr.m:321-323` sets `dc_source.source_state=true` inside a block covering BOTH the `eecon49_dual` and `decoupled_dual` families, so the decoupled family's shared GFL adapter becomes 11 states while its guard still expects 10; the earlier opt-in remedy that restored `test_ibr_decoupled_dual_mode_model` did not cover this profile. PROVEN PRE-EXISTING and independent of GATE-2026-08-25-01: with the five gate files reverted to HEAD and the DC-link work left in place, the same file still reports `6 run, 5 failed, 4 incomplete` with the identical error identifier, and the failing call chain contains none of the gate files because the error is raised during device construction before any candidate is evaluated. RESOLVED 2026-08-25: `scenario_ieee14_1sg_4ibr.m` now sets `dc_source.source_state=strcmp(model_id,'eecon49_dual')` -- the Thevenin state belongs to the EECON49 family only; the decoupled family keeps the algebraic source and its 10-state GFL adapter (both profiles verified to build with the right nx). The stale test assertions were then rewritten from MEASURED oracles: baseline 16->17, per-device 1:9 bit-identity in all-GFL, DC difference asserted by one-for-one root matching (state source 8 unmatched conjugate-pair roots vs algebraic 4 real roots), all-GFM active counts EQUAL at 11/converter with poles at -wD/-1/T_dc vs Thevenin pairs, washout-pole window on the REAL part at 1e-3 (FD residue), and `solve_profile` reference corrected from the TRIPPED machine (1) to the committed GFM converter (2) -- with ref=1 the gauge quotient displaced the washout poles entirely. `test_ieee14_decoupled_full_state` 6/6 PASS | [Admissibility gate rate vs ratio](2026-08-25-admissibility-gate-rate-vs-ratio.md) |
| GATE-2026-08-25-03 | RESOLVED | `handback_complete_time` reports the last step, not completion | Published `handback_complete_time=250` on a run whose C1 ramp demonstrably finished at 173.127 (`handback_t0=159.2519`, `handback_T=13.8752`, `alpha` first reaches 1 at exactly that time). `ts_simulate_ibr_hybrid.m:3885-3886` does not latch the branch: the controller stays active after completion, `a1` remains at 1, and every subsequent accepted step overwrites the field with its own `t+h`, so the published value is the final accepted sample. Reporting-only -- the release gate at `:1099` reads the BOOLEAN `handback_complete`, not the time -- but the field is quoted in report scalars and must be latched before any report cites it. Diagnostic cost was real: RESOLVED 2026-08-25: the completion branch now LATCHES `handback_complete_time` on the first transition (`if ~c.handback_complete`), so post-ramp stepping can no longer overwrite the completion instant with the final sample time; the boolean gate is untouched. Verified on a fresh 180-s production run: published value equals `actual_reclose_time + handback_duration_s` to 1e-3 (173.1271 = 159.2519 + 13.8752). An intermediate ordering that raised the flag BEFORE writing the time returned NaN -- recorded as a falsified first ordering; the flag must be raised AFTER the write, because the flag itself is the latch | [Admissibility gate rate vs ratio](2026-08-25-admissibility-gate-rate-vs-ratio.md) |
| GATE-2026-08-25-04 | RESOLVED | SG_ON authentication handed the validator a Y it could never match | After GATE-2026-08-25-01 made the all-GFL row `feasible=1 ready_to_commit=1` (verified in the table), the re-run outcome did NOT change: still `NO_FEASIBLE_SG_ON_ONE_STEP`, `actual_mode_reselection_time=NaN`, IBR2 still GFM at t=250, `reselection events: 0`. A SECOND independent gate was refusing. Found only after closing an observability defect: `choose_sg_online_one_step` discarded the validator's structured `err_id` at every `continue`, so the audit could publish only the aggregate status and an operator could not tell a missing table row from an unready row from a runtime rejection. A diagnostic `rejection_detail` cell (no gate reads it) published through `res.reselection_rejection_detail` answered it in one 180-s run: `release 2: validator refused (stability:gfm_selection:staleFingerprint)`. Layer-by-layer recomputation isolates it: input `d3c2efbf`->`4e153dba` MISMATCH, evidence `85256462`->`85256462` MATCH, table mismatch derived from input. The evidence layer matching exactly proves the criterion change did not invalidate the fingerprint. The topology payload differs by `max|stored-Ypre|=1.028245` with the OFF-DIAGONAL difference exactly `0` and the diagonal differing on precisely the eleven IEEE-14 load buses; `max|Ypre-stored-Yload|=8.6e-02` against `1.97` the other way identifies the gap as the constant-impedance LOAD admittance that `composite_dae` folds into the diagonal under `load_model='cz_p_cz_q'`, while `ibr_selector_table.m:101,390` hashed the load-free canonical Ybus (residual 8.6e-02 because the DAE converts load at the solved PF voltage, not `mpc.bus(:,8)`). The repository already documents the requirement: the note on `canonical_ybus_from_mpc` (`ts_simulate_ibr_hybrid.m:3663-3669`) says the helper exists to derive the runtime fingerprint "without the LOAD admittance term ... so the validator's input fingerprint matches the build-time hash", and `trip_transaction:1929-1933` complies -- which is why the `sg_trip` promotion at t=20 authenticates. Both SG_ON paths deviated: `authenticate_sg_on_candidate` used `Ytopo=Y` with the canonical derivation only as an `isempty` fallback (never taken), and `choose_sg_online_one_step` passed the live `Ycurr` straight through. So the SG_ON one-step release could not authenticate for ANY candidate in any configuration, independently of the damping criterion; defect -01 hid it because the release failed one gate earlier and the fingerprint layer was never reached. FIXED by deriving the payload in both SG_ON sites exactly as `trip_transaction` does. Not a weakening: `case_data.mpc` is never mutated at runtime (verified -- no assignment to `case_data.mpc` or `mpc.branch(:,11)` in the kernel; the scheduled line trip is a `Yline_stamp` delta against `Ybase_current` at `:56`), so the corrected SG_ON payload has exactly the drift sensitivity of the SG_OFF payload used all along, whereas the live load-folded Y gave unconditional failure -- a wall, not a gate. Every other gate stays in force: equilibrium convergence/conditioning, the damping-ratio criterion, physical-KCL norm, the exact one-step relation, hold/lockout revalidation, severity dwell, and the right-limit KCL solve. LIMITATION recorded: because `case_data.mpc` is static the canonical payload does not express an open scheduled branch, so a reselection attempted while the chronology line is open would authenticate against the intact-network hash -- a property of the existing contract shared with `trip_transaction`, inert here because the line is restored at t=145 before the reclose at 159.2519 and the release window after 173.13 | [Admissibility gate rate vs ratio](2026-08-25-admissibility-gate-rate-vs-ratio.md) |
| GATE-2026-08-25-05 | RESOLVED | The one-step accumulator had never executed | With -01 and -04 both fixed the validator authenticated and control reached the next statement, which threw `Subscripted assignment between dissimilar structures` at `choose_sg_online_one_step` `accepted(end+1,1)=c_auth`. `accepted` was initialised `repmat(struct(),0,1)`, a struct array with NO fields, so appending a populated candidate is invalid MATLAB; the line could never have executed because every prior iteration hit `continue` first -- via -01 (row not ready) or -04 (fingerprint mismatch). Fixed by accumulating into a cell (no field-set precondition) and materialising the struct array the frozen ranking indexes once the loop ends, normalising field sets with `orderfields` against the first accepted candidate and reporting a genuine mismatch through a new `candidateFieldMismatch` ID rather than padding it. TOGETHER -01/-04/-05 establish that the SG-online staged release from one grid-forming converter to zero has NEVER completed in this repository: three independent latent defects, each hiding the next, with no test covering the path end to end | [Admissibility gate rate vs ratio](2026-08-25-admissibility-gate-rate-vs-ratio.md) |
| GATE-2026-08-25-06 | RESOLVED_OWNER_DECIDED | The pinned arms are also subject to the post-reclose handback | Measured on the five-arm re-run: `pinned_gfm1` and `pinned_gfm2` both publish `reselection_status=NO_FEASIBLE_SG_ON_ONE_STEP`, so they DO enter the post-reclose severity release path. This falsifies the primary agent's own earlier statement in the same session that only the `adaptive` arm could be affected. Cause: `severity_handback_enabled` at `ts_simulate_ibr_hybrid.m:1446` is set from the presence of `healthy_pf_V`/`healthy_pf_bus_ids` ALONE, whereas `severity_support_enabled` additionally requires `automatic_support_supervision`; the pinned arms set that flag false, which disables the SG-OFF support supervisor but NOT the SG-ON handback. CONSEQUENCE needing an owner decision: now that the release can complete, an arm labelled "one grid-forming unit pinned, no run-time switching" will perform a run-time switch at t~=173, so its declared semantics no longer hold over the post-reclose window (they remain valid over the islanded window t in [20,159] where the comparison's substantive claims live). Two non-equivalent resolutions: (1) accept it and re-describe the arms -- the pin governs the ISLANDED configuration and every policy converges to all-GFL once the machine returns, changing only labels and report prose; or (2) make the pin hold across the reclose by disabling the severity handback on the pinned arms, which is an arm-definition change requiring a new option because `severity_handback_enabled` has no independent switch. Not decided by the agent. RE-RUN SCOPE CORRECTION: three arms depend on the release outcome, not one -- `adaptive`, `pinned_gfm1`, `pinned_gfm2`; `pinned_gfm4` (t_end=25.4880, `adaptiveDtMin`) and `locked_gfl` (t_end=20.0000, `noVoltageFormingSource`) terminate far short of the release window and their caches stay signature-valid because the runtime fixes do not touch the selector table or its fingerprint | [Admissibility gate rate vs ratio](2026-08-25-admissibility-gate-rate-vs-ratio.md) |
| DOC-2026-08-26-01 | RESOLVED | Thai switching report declared a state that no equation governed | `report_ieee14_switch_th_rev2.tex` listed the Thevenin source current as state 17 with active counts 10/11, but its shared-power-stage align row still substituted the algebraic closure `dVdc/dt=(1/C)[(Edc-Vdc)/Rdc-Pac/Vdc-Ich]` and `rg '\tau_s'` on the file returned ZERO matches: no equation anywhere governed the state the report's own table declared. Three further places were stale with it -- the monotone bound `Vdc<=max(Vdc(0),Edc)` argued from "every term of row (3) is negative", the small-signal prose predicting FOUR REAL roots just above `-100 1/s` from `lambda_dc=-100+10Pac/Vdc^2` with participation `1.000`, and `eq:oneway` written over `max_{i!=Vdc}|df_i/dVdc|` with a measured `17.65` -- while the same section `\input`s the generated table that prints four COMPLEX pairs at `-95.56..-98.41` with `zeta~0.707` near 15 Hz. Cause: the report was corrected by two passes that owned different parts of the file; `NUM-2026-08-20-01`/`GATE-2026-08-25-*` added the source state and rebuilt the English report with a new `sec:dcsource` subsection, but on the Thai side only the state table, state vector, active sets and reduction paragraph were updated, and nothing cross-checks an align row against the sentence beneath it or against an `\input`-ed table. The Thai file having no `sec:dcsource` and no `eq:dcblock` while the English file has both is the fingerprint. FIXED by porting the derivation from `+ibr/dc_source_thevenin_params.m` (not by translating the English prose): row (3) now reads `Idc`, new row (4) is the Thevenin source law, `Rdc` from the declared regulation with the reachability bound, `tau_s` from the maximally-flat criterion with both bounds and the degeneracy statement, the mechanism that replaces the retired bound, a stiffness paragraph, and the small-signal DC prose rewritten to the measured `0.500/0.500` split. FALSIFIED en route: that the English numbers were safe to translate (`realised margin 2.15` is the LEAST binding of four measured `2.0725..2.1454`, and the code comment's `2.32` matches no converter -- it used `P_ref` for `Pac0`; `Edc=1.0453` matches none either, measurement gives `1.0269..1.0448` because `x0(11)=Idc0=Pac0/Vdc0` makes `Pac0` exactly recoverable -- both corrected in BOTH reports); and that the first feasible selector row is the one `sssa_modes_n1.tex` prints (all four feasible `n=1` rows write that path so the LAST survives, `selected=2` not `selected=5`; measuring the wrong row gave `33.4019 -> 35.0558` and would have contradicted a correct English number). Oracle probe confirmed `physical_A 40x40`, `max|df_AC/dx_DC|=0` exactly, `max|df_DC/dx_AC|=33.4019`, DC sub-matrix block-diagonal with `0` outside the four 2x2 blocks, participation `0.5000/0.5000`. Gates: both reports built twice, TH 27 pp / EN 38 pp, 0 errors, 0 undefined, 0 overfull hbox; Thai pages 15 and 22 rasterised and inspected. No production code, test, case or generator touched | [Thai report DC state without equation](2026-08-26-thai-report-dc-state-without-equation.md) |
| DOC-2026-08-26-02 | OPEN | Production DC-source comment keeps a bound its own model breaks | `+ibr/dc_source_thevenin_params.m:123-134` still asserts `Vdc(t)<=max(Vdc(0),Edc)` from "every term of (2) is negative whenever Pac>=0", but equation (2) of that same header reads `Idc`, a STATE governed by (1), whose instantaneous value is not a function of `Vdc` -- so the sign argument does not follow, and a `zeta=1/sqrt2` pair overshoots its target by `4.32%`. Line 68 also states a realised discriminant margin of `2.32`, which matches no converter: measurement gives `2.0725/2.0811/2.0929/2.1454`, and `2.32` corresponds to `Pac0=0.85` (that is `P_ref`), inconsistent with the same header's own `Edc=1.0453` at line 134 which implies `2.148`. Comment-only, so NO numerical behaviour is affected and no gate is weakened -- `chopper_inactive_by_bound` is computed as `Edc<=Vdc_max` and is correct as named. Left OPEN rather than edited during a report-presentation pass because `+ibr/` is production path and this file is the project's authoritative derivation record; both switching reports now state the opposite of the first item and a measured range in place of the second | [Production DC comment stale bound](2026-08-26-production-dc-comment-stale-bound.md) |
| DOC-2026-08-26-03 | RESOLVED | `rev2` English report carries three claims its own generated inputs contradict | Reading every printed number back to the file that generates it found three stale prose claims in `report_ieee14_switch_en_rev2.tex`. (1) `:336-342` states the presented re-run "was gated on being bit-identical to the delivered artefact ... max|dx|=max|dy|=max|du|=0 exactly" and cites `\NewRunReclose` while doing so; that macro is defined in `figures/switch_ieee14_decision/run_summary_v2.tex:22`, whose own provenance header at `:3-9` states the opposite of its own numbers -- "This cache is NOT bit-identical to the earlier delivered artifact, and no such claim is made for it: the DC-link closure changed under NUM-2026-08-20-01 ... Compare it to the earlier cache by outcome, never by bit-identity." The gate was real for the IDEAL-DC cache under `AGSI-2026-08-16-01` and did not survive the model change; the identity that IS currently measured is the six arms agreeing exactly on the common window `[0,20)` s over 42 samples (`comparison_summary.tex:49-53`), a different and weaker statement about a different quantity. (2) Three cache paths are named for one result set -- `ieee14_gfm_lock_compare` at `:73` (ideal), `..._dcreal` at `:24`/`:320` (resistive Thevenin, 16 states), `..._zeta` in the macro file (inductive Thevenin, 17 states, the one actually printed); their reclose instants are `159.3436`/`159.2397`/`159.2519` respectively. (3) The IBR superset is "sixteen-state" at `:143`/`:217` against "17-state" at `:352`/`:734`, and the active counts are `9`/`10` with `A_GFL={1:9}`, `A_GFM={1:3,10:16}` at `:1173-1176` against `10`/`11` with `,17` in both sets at `:791-798` -- the latter being what `+ibr/eecon49_dual_mode_model.m:95-96` integrates. Same failure mode as `DOC-2026-08-26-01`: a model revision reached the sections that own the model and the generated inputs, but not every sentence elsewhere that had quoted the previous revision, and LaTeX cross-checks nothing between a sentence and the `\input`-ed file whose macro it cites. NO numerical impact -- all three are prose and every printed value is the correct current value because the values come from the macros; item 1 is the serious one because it advertises a verification gate that was not run on the presented trajectory. Resolved 2026-08-27: the report now states the current result provenance, the measured six-arm common-window identity, and the 17/10/11 state contract; two xelatex passes produced 38 pages and affected pages were visually inspected. The owner expanded the presentation pass to cover these proven stale report statements; no numerical model or gate changed. The deck authored in the same session states 17/10/11, names only the `_zeta` cache, and claims only the common-window identity | [rev2 stale provenance claims](2026-08-26-rev2-report-stale-provenance-claims.md) |
| TS-2026-09-03-01 | OPEN | Switched-TS adaptive stepper / GFM voltage-loop active set | Three of the four scenarios added in `b4d312c` stop before their 120 s horizon with `adaptiveDtMin`: `sg_fault_bus9` and `line_fault_9_14` at 50.083340 s (both BEFORE the clearing they were built to exercise) and `former_outage` at 60.706647 s (0.7 s AFTER its outage committed successfully). Classified by one-step replay from the exact accepted wall state on its own rebuilt topology, self-checked by KCL first (4.2e-14 / 1.8e-13, both inside `kcl_tol`). The coupled Newton returns `max|dx|=0` with `line_search_exhausted=1` at every step size at or above the `6.104e-6` floor, so the reported residual is the UNCHANGED iterate's and scales linearly with `h`; `rcond(J)` collapses to 1.5e-5 / 4.4e-6 at nominal `h` and recovers to ~1e-3 as `h` shrinks; the worst residual row is a voltage-loop integrator (`IBR2.gfm_xi_Vd`, `IBR3.gfm_xi_Vq`) not a network row; and the converters sit ON the current-reference limiter surface (`|I|/Imax` = 1.0045 and 1.0000). Same class as `TS-2026-08-13-03`, now evidenced on two topologies that record does not cover: a bolted fault ridden by a SINGLE former, and a post-outage island. `sg_fault_bus9` converges at `h=1e-6` -- below the floor the production run may legally take -- which falsifies static infeasibility THERE; the `former_outage` convergence at `h=1e-7` is weaker and is reported as such. NOT established: that the trajectory INTO the wall is healthy -- `former_outage` loads its three survivors to `P=0.446/0.996/0.755` pu with `f_COI` falling ~0.5 Hz/s, so a power-deficit collapse is equally consistent and the wall prevents deciding. The domain guard is ruled out as the mechanism (`domain_rejected_trials=0` at every failing `h`; a `domain_preserving_trials=false` control reproduces the failures with identical iteration counts), so the reading does not rest on `DOC-2026-08-28-02`'s unreproduced counter. Delivery consequence: the `line_fault_clear` handler added in the same commit is NOT exercised by any 120 s artifact and its evidence is the compressed-timeline KCL discrimination in `tests/test_line_fault_relay_clear.m` | [Scenario-suite adaptive dtMin wall](2026-09-03-scenario-suite-adaptive-dtmin-nonsmooth-wall.md) |
